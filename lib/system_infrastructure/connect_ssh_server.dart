import 'package:flutter/material.dart';

class ConnectSshServer extends StatelessWidget {
  const ConnectSshServer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
