import 'package:flutter/material.dart';
class ScanningPortDevice extends StatelessWidget {
  const ScanningPortDevice({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
